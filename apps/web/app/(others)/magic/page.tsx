import { VerifyMagic } from '@/sections';

export const metadata = {
    title: 'Verify Magic Login',
    description: 'Verification of magic login',
};

export default function VerifyMagicPage() {
    return <VerifyMagic />;
}
