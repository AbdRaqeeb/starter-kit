import { ResetPassword } from '@/sections';

export const metadata = {
    title: 'Create New Password',
    description: 'Set a new password for your account.',
};

export default function ResetPasswordPage() {
    return <ResetPassword />;
}
