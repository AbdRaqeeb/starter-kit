import { VerifyEmail } from '@/sections';

export const metadata = {
    title: 'Verify Your Email',
    description: 'Verify your email address to activate your account.',
};

export default function VerifyEmailPage() {
    return <VerifyEmail />;
}
