import { Dashboard } from '@/sections';

export const metadata = {
    title: 'Dashboard',
    description: 'Real time display of application activities',
};

export default function DashboardPage() {
    return <Dashboard />;
}
