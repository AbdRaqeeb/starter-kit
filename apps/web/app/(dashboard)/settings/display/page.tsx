import { DisplaySettings } from '@/sections';

export const metadata = {
    title: 'Settings Display',
    description: 'Display settings for the application',
};

export default function SettingsDisplay() {
    return <DisplaySettings />;
}
