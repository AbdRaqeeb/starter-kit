import { AccountSettings } from '@/sections';

export const metadata = {
    title: 'Account Settings',
    description: 'Update your personal information, security settings, and payment preferences.',
};

export default function AccountSettingsPage() {
    return <AccountSettings />;
}
